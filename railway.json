{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -f package.json ]; then npm run build; else cd web && npm run build; fi"
  },
  "deploy": {
    "startCommand": "if [ -f package.json ]; then npm run start:railway; else cd web && npm run start:railway; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
